# OS
.DS_Store
**/.DS_Store

# Editor / IDE
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Environment (secrets)
.env
.env.*

# Logs
*.log

# Node (if tooling is added later)
node_modules/

# Local tooling state (per-machine, not project code)
.ck/
.pi/
.specstory/

# Local data, scratch / experiments
scratchpad/
data/

# form.html is generated by build.mjs from src/ — keep it tracked
# (it is the shipped artifact). Edit src/, not form.html.